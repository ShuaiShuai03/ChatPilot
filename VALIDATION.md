# ChatPilot 验证记录

## 到达目标后误报并回退修复（2026-09-13）

用户反馈点击 ↑ 后已到达上一条消息，却退回原位置并提示「滚动尚未完成，请重试」。代码路径显示：精确对齐循环不断重置稳定计时，1.5 秒期限内未满足连续 160 毫秒静止就抛错，随后 `finally` 按失败路径恢复原位置。截图与该错误路径一致；未现场测量宿主页面的布局变化。

改为以目标正文进入可阅读区域判断成功；平滑滚动有界等待后，若目标已可读则停止剩余动画并保留目标位置。目标不可读时只进行一次补定位，再读取实际几何位置；仍不可读才报错。保留用户取消、会话隔离与真正失败时的恢复逻辑。

按用户此前要求不运行测试、类型检查或浏览器验收。本次执行生产构建和差异空白检查，结果见本次交付说明；历史测试通过记录不覆盖本修复。

## 悬浮导航稳定性修复（2026-09-13）

本节对应 `codex/fix-floating-scroll-stability`；下方旧测试记录仅对应历史版本，不能作为本次修复的测试结果。

代码审查定位到以下问题并完成修复：

- 滚动容器原先从会话根向上寻找且必须已有溢出，可能遗漏根内的实际容器或加载阶段的容器。现在从共享索引中的消息锚点寻找 CSS 滚动容器，并在索引变化和操作开始时更新绑定。
- 方向状态原先仅依赖输入事件和消息观察，惯性滚动或同一条长消息内的滚动可能不刷新边界。现在监听会话滚动并按动画帧合并更新。
- 连续跳转游标原先在位置变化超过 4px 后失效；虚拟列表的自动校正也会触发。现在保留消息身份游标，实际用户输入或列表定位再重置。
- 无持久外壳时，方向跳转原先先收集整段对话，可能来回滚动并因回答仍在生成而失败。现在已连接的邻近候选直接定位，仅在缺少可达候选时使用有界收集。
- 定位原先通过 `scrollIntoView` 移动祖先容器，且仅凭滚动停止判断成功。现在只操作选定会话容器、对齐消息开头，并在时限内处理正文重新挂载和位置偏移。API 行为参考 [MDN](https://developer.mozilla.org/en-US/docs/Web/API/Element/scrollIntoView)。
- 控制器解绑时同步清除操作状态，避免组件替换或新会话升级时遗留忙碌状态。
- 面板展开原先卸载右侧入口，使箭头横移 36px；现在保留不可见占位并调整窄屏宽度。透明区域不再拦截页面点击。
- 取消操作后保留内部操作锁，直到旧异步清理结束，防止旧样式恢复与新跳转交错。

本次按用户要求不运行单元测试、E2E、类型检查或浏览器验收。已执行 `pnpm build` 并成功生成 `.output/chrome-mv3/`，`git diff --check` 通过；读取生成的 manifest 确认权限仍只有 `storage`、内容脚本仍仅匹配 `https://chatgpt.com/*`。这些结果证明可构建，不代表已在生产会话中验证稳定性。重新加载扩展并刷新 ChatGPT 后才会使用新构建。

日期：2026-09-12

## 本轮交付

- 导航面板、弹窗、进度、错误、无障碍标签、扩展描述及工具栏标题改为简体中文。扩展 UI 根和弹窗使用 `zh-CN`；聊天原文保持原样。
- Markdown/TXT 用户角色标题为「用户」，无标题默认名称为「ChatGPT 对话」；JSON schemaVersion 1、字段及角色枚举保持兼容。
- 新增 36×36px 常驻 ↑↓ 按钮，面板收起与展开时均可使用。只按会话顺序定位用户消息，不受搜索、选择和导出范围影响；首尾不循环。
- 复用统一索引处理未知占位消息、虚拟化正文和 ID 迁移。手动阅读与列表定位会重置方向起点；操作期间禁用重复点击，并提供取消入口。
- 修复取消回到底部时，虚拟列表重绘改变高度造成恢复偏移的问题。恢复过程有时限；用户手动移动和会话切换会终止旧操作。

## 实际执行结果

| 检查 | 结果 |
| --- | --- |
| `pnpm typecheck` | 通过 |
| `pnpm test` | 6 个文件、41 项通过 |
| `pnpm test:e2e` | 生产构建后加载真实扩展，20 项通过，55.4 秒 |
| `pnpm build` | 通过；生成 `.output/chrome-mv3/` |
| 生产 manifest | 仅 `storage` 权限；内容脚本仅匹配 `https://chatgpt.com/*`；无后台脚本 |

原有 12 项 E2E 保留并更新为中文断言，包括搜索、选择、三种格式的实际下载与内容、虚拟化历史恢复、流式更新、分支替换、SPA、取消、不可达消息和设置同步。

新增 6 项 E2E 覆盖：收起状态连续上跳/下跳及边界、逐条发现未加载历史、搜索和选择独立性、列表点击后重定位、取消与恢复、手动滚动重定位、减少动态效果、快速额外点击以及跳转期间 SPA 替换。

布局在 1280、1440、1920px、浅色与深色模式下验证，检查按钮尺寸、与展开面板的间隔以及顶部和输入区边界。测试截图输出到 `test-results/chatpilot-{light|dark}-{width}.png`。已人工查看代表截图，并修正深色模式辅助文案对比度。

## 证据范围与本地状态

浏览器测试使用 Playwright Chromium 的临时配置加载生产扩展，以本地合成 HTML 替换 ChatGPT 文档并阻止其他网络请求。测试包含真实 DOM 滚动、虚拟化和下载；不是已登录生产 ChatGPT 上的新功能验收。ChatGPT 后续 DOM 变化仍可能需要适配。

以上为功能验证时的本地状态；Git 提交与远程同步状态以实际仓库记录为准。构建产物不纳入版本控制。

## 导出误取消修复（2026-09-12）

用户截图显示「你已移动会话，加载已停止」。复现测试模拟自动滚动后的页面延迟位置校正：旧实现因超过 120ms 且位置偏移大于 4px，误判为用户移动，导致下载中断；修复前该回归测试失败，修复后成功下载包含全部 6 条 ChatGPT 回答的 Markdown，并验证首尾内容及角色范围。

移除单凭滚动位置/时间推断用户操作的监听器；改为依据滚轮、触摸、指针和导航按键输入取消，并排除扩展 UI 内部交互。总时限、完整性检查、取消按钮和会话隔离保持有效。额外验证触摸、指针及导航按键仍能终止加载。

最终类型检查、41 项单元测试、20 项扩展 E2E 和生产构建全部通过。此回归在合成页面上验证，未对用户截图中的生产会话进行现场验收。已创建根目录 `AGENTS.md`，记录执行约定、项目结构、中文界面与兼容性要求、隐私边界、验证方式及外部操作授权规则。
